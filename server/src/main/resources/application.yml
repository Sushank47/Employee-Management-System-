spring:
  application:
    name: ems
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb+srv://Sushank:Sushank123@cluster0.fjfknpm.mongodb.net/employee_db?retryWrites=true&w=majority&appName=Cluster0}
      # Configures GridFS template name
      gridfs:
        bucket: ems-documents
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# Custom application properties
app:
  jwt:
    # 512-bit signing key generated for demonstration. Ensure this is replaced by a secure key in production.
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expirationMs: 86400000 # 24 Hours
  payroll:
    taxRate: 0.10 # default 10% tax rate

server:
  port: 8080

logging:
  level:
    root: INFO
    com.sushank.ems: DEBUG
    org.springframework.data.mongodb: DEBUG

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
